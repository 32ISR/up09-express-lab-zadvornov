info:
  name: users
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/api/admin/users/:id"
  params:
    - name: id
      value: "6"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
