info:
  name: create_review
  type: http
  seq: 11

http:
  method: POST
  url: "{{base_url}}/api/books/:id/reviews"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "rating": 4,
        "comment": "Отличная книга, очень увлекательно и хорошо написано."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
