info:
  name: register
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "username":"admin02",
        "password":"admin02",
        "email":"admin02@gmail.com",
        "role":"admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
