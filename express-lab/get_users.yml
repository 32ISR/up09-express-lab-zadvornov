info:
  name: get_users
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/api/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
