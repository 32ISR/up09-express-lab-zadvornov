info:
  name: books_byId
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/books/?id=1"
  params:
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
