info:
  name: profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/auth/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
